<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.joshua.r0th.azuretw"
    >


    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/azurenewlogo_round"
        android:label="@string/app_name"
        android:roundIcon="@drawable/azurenewlogo_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.DayNight.NoActionBar"
        tools:targetApi="31"
        android:requestLegacyExternalStorage="true"
        >
        <activity
            android:name=".splashCheck"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
        <activity android:name=".dozemanager"
            android:theme="@style/Theme.AppCompat.NoActionBar"
            android:noHistory="true"
            android:launchMode="singleTop"/>

        <activity android:name=".thermalmanager"
            android:theme="@style/Theme.AppCompat.NoActionBar"
            android:noHistory="true"
            android:launchMode="singleTop"/>

        <receiver android:name=".widget.Azure_widget_provider" android:exported="true" >
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data android:name="android.appwidget.provider"
                android:resource="@xml/status_widget" />
        </receiver>


    </application>

</manifest>